# MongoDB URI
MONGO_URI=mongodb://localhost:27017/quickbite

# Port the User Service will run on
PORT=5001

# Secret key for JWT authentication (if using)
JWT_SECRET=mysecretkey

# Optional: other configuration settings (like email services, etc.)
# EMAIL_SERVICE=your-email-service
